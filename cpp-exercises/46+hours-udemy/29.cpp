#include <iostream>

/* Write a program that reads in two lists of integers from
the user and stores them in vectors. The program should then
compute the intersection and union of the two lists and print
the resulting vectors. */

int main(void) {

}
